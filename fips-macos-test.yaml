# FIPS macOS BLE Central (Scan Mode)

node:
  identity:
    persistent: true

tun:
  enabled: false

dns:
  enabled: false

transports:
  ble:
    enabled: true
    advertise: false
    accept_connections: false
    scan: true
    auto_connect: true

logging:
  level: debug
  format: pretty
